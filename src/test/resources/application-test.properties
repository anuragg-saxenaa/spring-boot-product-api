# Test profile overrides

# Do not require external services for unit tests
spring.kafka.enabled=false
spring.redis.host=localhost

# Completely disable Spring Kafka auto-configuration (prevents AdminClient from trying localhost:9092)
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.kafka.KafkaAutoConfiguration,org.springframework.boot.autoconfigure.kafka.KafkaReactiveAutoConfiguration
